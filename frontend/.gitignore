node_modules
dist
.vite
*.local
*.tsbuildinfo

# MSW worker is generated by `pnpm dlx msw init public/ --save`
# We keep it gitignored — runtime artifact, not source.
public/mockServiceWorker.js

# This is a pnpm project. If `package-lock.json` ever appears it's npm
# accidentally being run here — committing it would create dual-lockfile
# inconsistencies. The lockfile of record is `pnpm-lock.yaml` at repo root.
package-lock.json
